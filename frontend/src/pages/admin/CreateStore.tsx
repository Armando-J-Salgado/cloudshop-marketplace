import React from "react"
import { PagePlaceholder } from "@/components/common/PagePlaceholder"

export function CreateStore() {
  return (
    <PagePlaceholder
      title="Crear Nueva Tienda"
      module="Tiendas (Módulo 3)"
      status="Sin backend"
      description="Formulario para registrar una nueva tienda en la infraestructura CloudShop."
    />
  )
}
